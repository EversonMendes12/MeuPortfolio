name: Build and Deploy Angular App

on:
  push:
    branches: ["master"]
  workflow_dispatch:

permissions:
  contents: write  # Adiciona permissão de escrita para conteúdos do repositório
  pages: write  # Adiciona permissão de escrita para GitHub Pages
  id-token: write  # Necessário para GitHub Pages

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '16'

      - name: Install dependencies
        run: npm install
      
      - name: Build the Angular app
        run: npm run build -- --output-path=dist

      - name: Upload artifact
        uses: actions/upload-artifact@v3
        with:
          name: angular-build
          path: dist/

  deploy:
    needs: build  # Garante que o job de deploy só execute após o build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download build artifact
        uses: actions/download-artifact@v3
        with:
          name: angular-build
          path: dist/

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
